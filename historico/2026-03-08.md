# Snapshot - 08/03/2026

## O que foi feito hoje

### Sprint 6 — Estética, Navegação e Excel

#### Melhorias Estéticas (app inteiro)
- `globals.css`: keyframes customizados (fade-in-up, fade-in, float, gradient-shift, shimmer, pulse-ring)
- Classes utilitárias: `.animate-fade-in-up`, `.animate-float`, `.gradient-text`, `.card-hover`, `.delay-*`
- Hero: blobs animados com `animate-float`, badge com dot pulsante, headline com `.gradient-text` animado, CTAs com shadow dinâmico
- Features: hover effect com gradiente colorido por card, scale no ícone, lift -translate-y-1.5
- HowItWorks: linha conectora com gradiente (green→blue→purple→orange), ícones coloridos por passo, hover scale + color
- Header: detecção de rota ativa (`usePathname`) com underline indicator, scroll effect (shadow ao rolar), menu mobile animado
- Calculadora: summary cards com animação staggered + ícones coloridos, lista de operações com hover que mostra o botão deletar, botão "nova operação" dashed
- GraficoResumo: mantido mas movido para /historico

#### Navegação Reestruturada
- Calculadora: foco em registrar/calcular, sem gráficos pesados
- Nova rota `/historico`: summary chips + GraficoResumo + tabela mensal + PortfolioCard
- Header: link "Histórico" aparece apenas para usuários logados
- Dropdown do usuário: "Histórico & Gráficos" com ícone BarChart2
- Calculadora: botão "Ver Histórico & Gráficos completos" no final da lista

#### Suporte a Excel (.xlsx)
- Instalado: `xlsx@0.18.5` (SheetJS)
- `pdf-parser.ts`: nova função `parseBinanceXLSX(rows)` — detecta colunas por nome (PT-BR e EN), parse de data (string e serial Excel)
- `upload-csv.tsx`: nova função `handleXLSX()` com dynamic import do xlsx, roteamento para `.xlsx`/`.xls`
- Input file: `accept=".csv,.pdf,.xlsx,.xls"`
- Texto atualizado: "Binance PDF e Excel (.xlsx) · 9 exchanges via CSV"

## Decisões técnicas
- GraficoResumo removido da calculadora (menos peso na tela principal)
- xlsx importado dinamicamente para não aumentar o bundle inicial
- Animações via CSS puro (não biblioteca externa) — compatível com SSR
- `animate-*` usam `animation: ... both` para manter estado final sem flash

## Estado atual
- Build: ✅ 0 erros, 0 warnings críticos
- Rotas: 20 (adicionado /historico)
- App visual: muito mais dinâmico — animações, hover effects, gradientes

## Próxima sessão
- Testar Excel da Binance com arquivo real
- Avaliar animações em mobile (performance)
- Avaliar adicionar landing page com Pricing CTA mais chamativo
