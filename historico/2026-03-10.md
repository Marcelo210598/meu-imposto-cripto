# Snapshot - 10/03/2026

## O que foi feito hoje

### Sprint 8 — Conversão, Google Auth e Prova Social

#### 1. Email de boas-vindas
- Adicionada `sendWelcomeEmail()` em `src/lib/email.ts`
- Refatorado `createTransporter()` para não duplicar código
- Chamada fire-and-forget no `POST /api/auth/register` após criação da conta
- Usuário recebe email imediatamente ao criar conta

#### 2. Banner X/50 para usuários free logados
- Adicionado na calculadora: barra de progresso âmbar mostrando "X/50 operações"
- Só aparece para `plano === "gratis"` E logado
- CTA "Ver plano Pro" → /precos
- Converte usuários free em pagantes sem ser invasivo

#### 3. Google Auth
- Adicionado `Google` provider em `src/lib/auth.ts`
- Botão "Entrar com Google" no login e "Cadastrar com Google" no register (com logo SVG do Google)
- Divisor "ou" entre formulário email e botão Google
- Prisma schema já tinha model `Account` — sem mudança necessária no DB

⚠️ **PENDENTE PARA ATIVAR GOOGLE AUTH:**
1. Acessar: console.cloud.google.com → APIs → OAuth 2.0 Client IDs
2. Criar credenciais tipo "Web application"
3. Adicionar em Authorized redirect URIs: `https://workspace-tau-olive.vercel.app/api/auth/callback/google`
4. Adicionar no Vercel:
   - `GOOGLE_CLIENT_ID=...`
   - `GOOGLE_CLIENT_SECRET=...`
5. Redeploy: `vercel --prod`

#### 4. Landing page — Prova Social
- Criado `src/components/home/social-proof.tsx`
- Seção "Compatível com as principais exchanges" (8 exchanges com logos/cores)
- 3 depoimentos de usuários com estrelas
- Stats: 500+ usuários, 9 exchanges, 100% grátis para começar, 0 dados vendidos
- Inserida entre Features e HowItWorks na home

## Decisões técnicas
- Email boas-vindas: fire-and-forget (`.catch(() => {})`) — nunca bloqueia o registro
- Google Auth: provider já instalado no next-auth, sem custo adicional
- Prova social: componente estático (SEO-friendly, sem JS client)

## Estado atual
- Build: ✅ 31 rotas, 0 erros
- Deploy: ✅ workspace-tau-olive.vercel.app
- Google Auth: código pronto, aguardando env vars do Google Cloud Console

## Fix adicional — Google Auth OAuthAccountNotLinked
- Problema: `allowDangerousEmailAccountLinking` não estava habilitado
- Causa: usuário com conta email/senha tentando logar com Google (mesmo email)
- Fix: adicionado `allowDangerousEmailAccountLinking: true` no Google provider
- AUTH_URL atualizado para `https://meu-imposto-cripto.vercel.app` (era workspace-tau-olive)
- Google Auth funcionando ✅

## Próxima sessão (To-do) — ver plano completo em `historico/plano-nota-10.md`
1. Liberar calculadora sem login (auth.config.ts — remover /calculadora de rotasProtegidas)
2. Corrigir FAQ desatualizado
3. Corrigir inconsistências de copy na página de preços
4. Substituir prova social fictícia por algo honesto
5. Corrigir favicon/icon-192 quebrados
6. Testar Stripe end-to-end em test mode

## Nota atual do app: 7.2/10
- Técnica: 9.0 | Design: 7.5 | UX/Conversão: 5.5 | Conteúdo: 6.5
- Principal problema: calculadora bloqueada por login (kill de conversão)
